# Meme Sentiment Labeling

Two-condition labeling app (Baseline vs With-AI) with Flask + vanilla JS.

## Run locally
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
